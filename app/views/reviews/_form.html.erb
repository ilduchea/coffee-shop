<%= render "layouts/errors", :object => @product %>

<%= form_for [@product, Review.new], remote: true do |f| %>
  <div class="form-group">
    <%= f.text_area :body, class: "form-control", placeholder: "Your Review" %>
  </div>
  <%= f.submit class: "btn btn-inverse" %>
<% end %>
